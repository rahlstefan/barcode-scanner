name: barcode_scanner_flutter
description: DataMatrix detector — Flutter UI + native iOS AVFoundation/Vision + TFLite int8 (YOLO26n).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/yolo26n_320_multiclass_no_mosaic_tail_20260512_073544_float16.tflite
    - assets/models/train_latency_yolo26n_320_auto_float16.tflite
